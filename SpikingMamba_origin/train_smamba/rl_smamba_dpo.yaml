# Model arguments
model_name_or_path: ./output_dir_distill # your distilled spiking mamba model path
torch_dtype: bfloat16

# Data training arguments
# For definitions, see: src/h4/training/config.py
dataset_mixer:
  JunxiongWang/llama3-ultrafeedback-armorm: 1.0
dataset_splits:
- train_prefs
- test_prefs
preprocessing_num_workers: 64

# DPOTrainer arguments
bf16: true
beta: 0.1
do_eval: true
evaluation_strategy: steps
eval_steps: 2000
gradient_accumulation_steps: 1
gradient_checkpointing: true
gradient_checkpointing_kwargs:
  use_reentrant: False
learning_rate: 5.0e-4
log_level: info
logging_steps: 10
lr_scheduler_type: cosine
max_length: 1024
max_prompt_length: 512
num_train_epochs: 3
optim: adamw_torch
output_dir: ./output/smamba_dpo/
per_device_train_batch_size: 4
per_device_eval_batch_size: 8
push_to_hub: false
save_strategy: "steps"
save_steps: 2000
save_total_limit: 2
seed: 42
warmup_ratio: 0.1
report_to:
- wandb